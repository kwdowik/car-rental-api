sut:
  env_file:
    - ".env.staging"
  build: .
  command: /bin/sh run_tests.sh
  depends_on:
    - mongo
  links:
    - mongo
    

mongo:
  container_name: mongo
  image: mongo
  volumes:
    - ./data:/data/db
  ports:
    - "27017:27017"